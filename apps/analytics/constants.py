# ────────────────────────────────────────────────────────────────────────
# apps/analytics/constants.py — константы модуля аналитики.
#
# Все магические числа и строки вынесены в один файл для DRY.
#
# ЧТО БУДЕТ, ЕСЛИ УДАЛИТЬ ФАЙЛ:
#   • ImportErrors везде: PERIOD_DAILY, MAX_TOP_PRODUCTS, ...
#   • Сервисы не смогут определять периоды агрегации
# ────────────────────────────────────────────────────────────────────────

# ────────────────────────────────────────────────────────────────────────
# ПЕРИОДЫ АГРЕГАЦИИ (для отчётов и графиков)
# ────────────────────────────────────────────────────────────────────────

# Период — размер «корзины» для агрегации данных.
# Например: PERIOD_DAILY → группировка по дням (30 точек на графике).

PERIOD_HOURLY = 'hourly'
PERIOD_DAILY = 'daily'
PERIOD_WEEKLY = 'weekly'
PERIOD_MONTHLY = 'monthly'

PERIOD_CHOICES = (
    (PERIOD_HOURLY, 'По часам'),
    (PERIOD_DAILY, 'По дням'),
    (PERIOD_WEEKLY, 'По неделям'),
    (PERIOD_MONTHLY, 'По месяцам'),
)

# Количество периодов по умолчанию для графиков.
# 30 дней / 12 недель / 12 месяцев — оптимально для дашборда.
DEFAULT_DAILY_POINTS = 30
DEFAULT_WEEKLY_POINTS = 12
DEFAULT_MONTHLY_POINTS = 12

# ────────────────────────────────────────────────────────────────────────
# ЛИМИТЫ
# ────────────────────────────────────────────────────────────────────────

# Сколько топ-товаров возвращать по умолчанию.
# Топ-10 — стандарт для дашборда (хватает для обзора).
MAX_TOP_PRODUCTS = 50
DEFAULT_TOP_PRODUCTS = 10

# Сколько топ-категорий возвращать.
DEFAULT_TOP_CATEGORIES = 10

# Сколько топ-покупателей возвращать.
DEFAULT_TOP_CUSTOMERS = 10

# Максимальная длина названия события (для ProductView.session_key и т.д.)
MAX_SESSION_KEY_LENGTH = 40
MAX_USER_AGENT_LENGTH = 500

# ────────────────────────────────────────────────────────────────────────
# ТИПЫ СОБЫТИЙ АНАЛИТИКИ
# ────────────────────────────────────────────────────────────────────────

# ИСТОЧНИК события — откуда пришёл просмотр.
# organic — прямой заход (через поиск на сайте, закладка)
# search — поисковая система (Google, Яндекс)
# social — соцсети
# referral — реферальная ссылка
# direct — прямая ссылка
SOURCE_ORGANIC = 'organic'
SOURCE_SEARCH = 'search'
SOURCE_SOCIAL = 'social'
SOURCE_REFERRAL = 'referral'
SOURCE_DIRECT = 'direct'

SOURCE_CHOICES = (
    (SOURCE_ORGANIC, 'Органический'),
    (SOURCE_SEARCH, 'Поисковик'),
    (SOURCE_SOCIAL, 'Соцсети'),
    (SOURCE_REFERRAL, 'Реферал'),
    (SOURCE_DIRECT, 'Прямой'),
)
